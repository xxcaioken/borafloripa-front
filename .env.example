# URL do backend em produção (Azure App Service)
# Ex: https://bora-back.azurewebsites.net
# Deixar vazio em dev (Vite faz o proxy automaticamente)
VITE_API_BASE_URL=

# URL do backend local (opcional, padrão: http://localhost:8000)
VITE_DEV_BACKEND_URL=http://localhost:8000
